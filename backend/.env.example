# MongoDB Connection String
# Replace with your MongoDB Atlas connection string or local MongoDB URL
# Important: Do NOT use '@' symbol in your database password
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/Food_Delivery_DB

# JWT Secret Key
# Use a strong random string for production (e.g., generate using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=your_super_secret_jwt_key_here_minimum_32_characters

# Stripe Secret Key
# Get this from your Stripe Dashboard: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here

# Server Port (optional, defaults to 4000)
PORT=4000

# Frontend URL (for CORS and Stripe redirects)
FRONTEND_URL=http://localhost:5173
